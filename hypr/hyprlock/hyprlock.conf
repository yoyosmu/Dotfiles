general {
    hide_cursor = true
    ignore_empty_input = true
    fail_timeout = 2000
}

background {
    monitor =
    path = /home/yoyomu/dotfiles/hypr/hyprpaper/background.jpg
    blur_size = 4
    blur_passes = 2
    brightness = 0.7
}

label {
    monitor =
    text = cmd[update:60000] echo "$(date +"%A, %B %d")"
    color = rgba(232, 240, 225, 0.9)
    font_size = 16
    font_family = Montserrat
    font_weight = 600
    position = 40, -40
    halign = left
    valign = top
}

label {
    monitor =
    text = 󰤄
    color = rgba(232, 240, 225, 0.85)
    font_size = 18
    font_family = Montserrat
    position = -100, -40
    halign = right
    valign = top
    onclick = systemctl suspend
}

label {
    monitor =
    text = 
    color = rgba(232, 240, 225, 0.85)
    font_size = 18
    font_family = Montserrat
    position = -60, -40
    halign = right
    valign = top
    onclick = systemctl reboot
}

label {
    monitor =
    text = 
    color = rgba(232, 240, 225, 0.85)
    font_size = 18
    font_family = Montserrat
    position = -20, -40
    halign = right
    valign = top
    onclick = systemctl poweroff
}

shape {
    monitor =
    size = 380, 420
    color = rgba(15, 25, 15, 0.55)
    rounding = 20
    position = 0, 0
    halign = center
    valign = center
}

image {
    monitor =
    path = /usr/share/sddm/themes/pixie/assets/avatar.jpg
    size = 120
    rounding = -1
    border_size = 0
    position = 0, 140
    halign = center
    valign = center
}

label {
    monitor =
    text = $USER
    color = rgba(232, 240, 225, 1)
    font_size = 16
    font_family = Montserrat
    font_weight = 700
    position = 0, 60
    halign = center
    valign = center
}

input-field {
    monitor =
    size = 200, 30
    outline_thickness = 2
    dots_size = 0.25
    dots_spacing = 0.35
    dots_center = true
    outer_color = rgba(166, 227, 161, 0.4)
    inner_color = rgba(0, 0, 0, 0.35)
    font_color = rgba(232, 240, 225, 1)
    fade_on_empty = false
    rounding = 10
    check_color = rgba(166, 227, 161, 1)
    fail_color = rgba(255, 85, 85, 0.9)
    placeholder_text = Enter Password
    fail_text = Wrong ($ATTEMPTS)
    hide_input = false
    position = -17, -20
    halign = center
    valign = center
}

shape {
    monitor =
    size = 30, 30
    color = rgba(166, 227, 161, 0.85)
    rounding = 10
    position = 103, -20
    halign = center
    valign = center
}

label {
    monitor =
    text = →
    color = rgba(15, 25, 15, 1)
    font_size = 16
    font_family = Montserrat
    font_weight = 700
    position = 103, -20
    halign = center
    valign = center
}
